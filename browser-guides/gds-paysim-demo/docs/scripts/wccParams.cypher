:param bigFirstPartyFraudGroups=>apoc.cypher.runFirstColumnSingle('MATCH (c:Client) WHERE c.firstPartyFraudGroup IS NOT NULL WITH c.firstPartyFraudGroup AS firstPartyFraudGroupId,collect(c.id) AS group WITH *, size(group) AS groupSize WHERE groupSize >= 3 RETURN collect(firstPartyFraudGroupId) AS bigFirstPartyFraudGroups',{});
